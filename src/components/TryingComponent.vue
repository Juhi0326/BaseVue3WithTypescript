<template>
    <!-- <div v-if="sm || xs">this is small size</div>
    <div v-else>this is not small size</div> -->
</template>
  
<script setup lang="ts">
/* import { useDisplay } from 'vuetify'
import { ref, watch } from 'vue';

// Destructure only the keys you want to use
const { sm, xs } = useDisplay()
const isSm = ref(sm);
const isXs = ref(xs);

watch(isSm, (newVal) => {
  if (newVal) {
    console.log('sm is true'); // This message will be logged to the console
  } else {
    console.log('sm is not true');
  }
});
watch(isXs, (newVal) => {
  if (newVal) {
    console.log('xs is true'); // This message will be logged to the console
  } else {
    console.log('xs is not true');
  }
}); */
</script>